import React from "react";
import styled from "styled-components";

function Main() {
  return (
    <MainWrapper>
      test
    </MainWrapper>
  );
}

const MainWrapper = styled.div`
  
`;

export default Main;
